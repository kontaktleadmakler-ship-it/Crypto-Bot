# v25.0.5 – ML Quality & Recovery Hardening

## Included
- TensorFlow.js validation diagnostics: label counts and predicted class counts.
- Explicit ML validation quality state (`LOW_SAMPLE`, `WEAK_POSITIVE_DETECTION`, `WEAK_DISCRIMINATION`, `USABLE_SHADOW_ONLY`).
- Dataset quality log with sample/win/loss counts.
- Historical ML recovery v3 diagnostics with unresolved reason counters and adapter order count.
- Runtime/log/status version labels consolidated to v25.0.5 without renaming legacy filenames.
- Existing v25.0.1 instance-lock hardening, v25.0.2 ML recovery changes, and v25.0.3 TFJS EarlyStopping fix retained.

## Safety
- No fabricated ML features are introduced.
- No exchange/live-order execution is enabled.
- ML quality is diagnostic only; the existing fail-closed behavior remains.
- DQN/RL2 remains enabled.

## Expected startup diagnostics
`[TensorFlow.js] Dataset-Qualität: samples=... wins=... losses=...`

`[TensorFlow.js] Validation-Set: samples=... wins=... losses=...`

`[TensorFlow.js] Validation-Diagnose: labels=+.../-... predictions=+.../-... quality=...`

`[ML-RECOVERY] v3 complete ... unresolvedReasons={...}`
